<% if session[:user_id] == @user.id %>
  <% if entry.note.nil? || entry.note.empty?%>
    <%= link_to "Add Note", edit_entry_path(entry) %>
  <% else %>
    <%= link_to 'Edit Note', edit_entry_path(entry) %>
  <% end %>
<% end %>