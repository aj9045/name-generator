this is where a list of all users shall go
<ol>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user_path(user.id) %>
      <ul>
        <li>saved names: <%= user.entries.count %></li>
      </ul>
  <% end %>
</ol>